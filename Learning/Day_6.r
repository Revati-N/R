# Preprocessing Data

?is.na
example <- c("A",1,6,7,NA,"B")
example

mean(example)

is.na(example) # Tells whether any of it is NA / Missing

length(example)

mean(example,na.rm = TRUE)

median(example,na.rm = TRUE)

1/0 # Infinity

